160:$U$-statistics are unbiased and statistically efficient estimators that can capture pairwise and higher-order interaction structure, making them fundamental to a wide range of statistical problems. Representative examples include the Gini index \citep{yitzhaki2003gini} for measuring imbalance; 
172:When covariates $X_1,\ldots, X_n$ drawn from $\mathbb{F}_X$ are available, a natural alternative is to leverage a machine learning model ${\mu}(X)$, which predicts the unknown label $Y$. Let $\hat{Y}_i={\mu}(X_i)$, $i\in[n]$ be the predictions. A naive approach is to directly substitute these predictions for the true labels when computing the target $U$-statistic. Such plug-in strategies are common in medical diagnostics \citep{bhavsar2021medical}, social science \citep{grimmer2021machine}, and financial decision-making \citep{gu2020empirical}, where labels are expensive or delayed. However, such plug-in estimators are generally biased and can exhibit uncontrolled variance, particularly when the predictive model is misspecified or inaccurate.
202:\paragraph{$U$-statistics} $U$-statistics have been extensively studied as a general framework for estimating functionals defined by pairwise or higher-order kernels. Foundational work established their unbiasedness and asymptotic behavior \citep{Hoeffding1948class,serfling2009approximation,peel2010empirical}, while more recent research has focused on scalability, inference, and learning-theoretic properties. 
215:%We first propose an unbiased augmented inverse probability weighting (AIPW) $U$-statistic that incorporates the sampling rule and machine learning predictions. We then derive the optimal sampling rule for the AIPW $U$-statistic. Building on this, we introduce the active $U$-statistic, establishing its asymptotic distribution and consistent variance estimation for valid statistical inference.
223:Since the sampling rule $\pi(\cdot)$ induces selection bias, $U\left(\{Y_i:\xi_i=1\}\right)$ as in \eqref{eq:U} is biased for $\theta^*$.
231:Regarding the fact that the plug-in estimator $U(\hat{\mathcal{Y}}_n)$ is biased, we correct its bias using an IPW term and define the AIPW $U$-statistic as
232:%introduce the bias-correction estimator of $U$-statistic via directly plugging in $\hat{Y}$.
239:%$U^{\pi}_{\rm AIPW}$ is unbiased because the second term provides an IPW correction to the plug-in component in the first term.
240:%The unbiasedness of $U^{\pi}_{\rm AIPW}$ can be evaluated by the bias-correction operation.
242:\begin{proposition}[Unbiasedness of the AIPW $U$-statistic]\label{prop:unbiased_aipw_u}
317:Since $\E[\widehat{N}]=\binom{n}{r}$, $\widehat{N}$ is an unbiased estimator of the total number of $r$-tuples and therefore provides a natural random normalization. 
328:Theorem~\ref{thm:unbiased_aipw_u} shows that $U^{\pi, N}_{\rm AIPW}$ is asymptotically unbiased for $\theta^*$. 
332:\begin{theorem}[Asymptotic unbiasedness of the normalized AIPW $U$-statistic]\label{thm:unbiased_aipw_u}
333:The normalized AIPW $U$-statistic $U^{\pi, N}_{\rm AIPW}$ is asymptotically unbiased for
517:The first term uses predicted labels and is generally biased, while the second term provides an IPW correction based on actively acquired labels. 
518:This yields an unbiased estimator of the population $U$-risk.
746:This $U$-statistic is an unbiased estimator of the parameter
762:\subsection{Political bias dataset}
768:    \caption{Political bias dataset. Estimation of Kendall's $\tau$ to detect whether LLM-based labels distort the underlying ordinal structure and thereby bias downstream audits. \textbf{Left}: 
770:   % Political bias dataset. Average intervals in five randomly chosen trials (left), empirical coverage (middle), and effective sample size (right) for Kendall's $\tau$ used to detect whether LLM-based labels distort the underlying ordinal structure and thereby bias downstream audits.
775:We further evaluate our method on a \textbf{political-bias} task using online news articles annotated for {political leaning}. Following the setup in \citet{gligoric2025can,Li2025Robust}, each sample consists of article text and metadata (topic, source, title, date, URL) with a ground-truth label
776:%We further evaluate our approach on a {political bias evaluation task}, using a corpus of online news articles annotated for \emph{political leaning}. Following the prior setup \citep{gligoric2025can,Li2025Robust}, each observation corresponds to a news article and includes metadata (e.g., topic, source, title, date, and URL) together with the article text. The ground-truth label
781:%indicates the political leaning of the article/source. In addition, we leverage large language models (LLMs) to produce \emph{model-based labels} and associated confidence scores (e.g., GPT-4), which can be treated as proxy annotations when high-quality labels are expensive or limited.
785:To this end, we measure the agreement between the true label $Y^{\rm TR}$ and the LLM prediction $Y^{\rm LLM}$ from GPT-3.5 using Kendall's $\tau$ coefficient \citep{kendall1948rank}, a classical second-order $U$-statistic. After encoding the ordered categories as an ordinal score (e.g., $\texttt{left} < \texttt{center} < \texttt{right}$), the target parameter can be written as
790:%In our experiments, $Y^{\rm LLM}$ is obtained from an LLM (e.g., GPT-3.5), while $Y^{\rm TR}$ denotes the dataset label. 
794:%A small or near-zero $\theta^*$ indicates frequent reversals in left–right comparisons, suggesting that LLM-based labels may distort ordinal structure and mislead downstream bias audits. Since high-quality political-leaning annotations are costly and limited, this task naturally motivates active label acquisition.
1009:\subsection{Proof of Proposition \ref{prop:unbiased_aipw_u}}
1107:\subsection{Proof of Theorem \ref{thm:unbiased_aipw_u}}
1421:In all of our experiments, we treat the estimates obtained from the full dataset as the true value. In each trial, the underlying data points are fixed, and the randomness arises from the labeling decisions $\xi_i$. The results are averaged over 3000 trials to obtain the final estimate. In the Income and Perioperative datasets, the predictive model $\mu$ and the uncertainty function $V(x)$ are both estimated using the XGBoost regression model. And for the Political Bias dataset, the predictive model $\mu$ is based on the predictions of GPT-3.5, while the uncertainty function $V(x)$ is estimated using the predictions from GPT-4.
1435:Using the same data to both train the predictive model $\mu$ and evaluate its predictions may introduce bias. In the Income and Perioperative datasets, the total numbers of labeled samples used are $m_{\text{Income}}=1000$ and $m_{\text{Perioperative}}=760$, respectively.
1436:To address the potential reuse bias, we employ a two-fold scheme, in which the queried dataset $\mathcal{D}=\{(X^\prime_k,Y^\prime_k)\}_{k=1}^m$ is divided into two folds. 
1863:% Previous contributors include Dan Roy, Lise Getoor and Tobias

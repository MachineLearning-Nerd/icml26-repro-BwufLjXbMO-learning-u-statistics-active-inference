# Claim 1 — setup protocol

Target live claim: the AIPW U-statistic is unbiased (Proposition 2.1).

Attempt 1 will derive the estimator from the pinned TeX source, implement a finite binary-outcome clean-room enumeration, and compare its expectation over outcome and sampling assignments with the target U-statistic. It will include a deliberately omitted inverse-probability-weighting control expected to be biased under non-uniform sampling. This is a setup protocol only; no verdict is asserted.
